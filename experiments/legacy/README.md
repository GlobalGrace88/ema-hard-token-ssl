# Legacy experiment orchestrators

Legacy DINO/BYOL trainers, `run_v4_*` queues, MSD zero-shot eval, and root ImageNet `train.py` are **not** included in this public release.

For historical reference, see the private development repository.

Tier-2 may add nnFormer backbone support and additional orchestration.
